<?xml version="1.0" encoding="utf-8"?>
<LinearLayout android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/topAppBar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:title="@string/app_name"
        app:titleCentered="true"
        app:titleTextColor="@color/white"
        android:background="@color/orange_primary_color"
        />
    
    <ImageView
        android:id="@+id/jurassic_park_image"
        android:contentDescription="@string/image_jurassic_park_description"
        android:layout_width="match_parent"
        android:layout_height="@dimen/height_250dp"
        android:src="@drawable/jurassic_park"
        android:scaleType="centerCrop"
        android:layout_marginTop="@dimen/margin_top_100dp"/>

    <TextView
        android:layout_width="match_parent"
        android:textAlignment="center"
        android:textStyle="bold"
        android:layout_marginTop="@dimen/margin_top_50dp"
        android:layout_height="wrap_content"
        android:textSize="@dimen/text_size_16sp"
        android:text="@string/movie_name"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</LinearLayout>